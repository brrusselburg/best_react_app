import React from 'react';

const Mystery = () => {
// who knows what this will be! not me

  return (
    <div className={classes.counter}>
      <h2 className={classes.header}>Mystery Button</h2>
      <button
        className={classes.button}
        type="button"
        onClick={incrementCounter}>
        ??????
      </button>
    </div>
  );
};

export default Mystery;
